android {
            defaultConfig {
                applicationId = "org.nativescript.shapp"

                if (false) {
                    minSdkVersion = false
                }

                if (false) {
                    targetSdkVersion = false
                }
            }
        }